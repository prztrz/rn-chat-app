// @flow strict

import * as React from 'react';
import { View } from 'react-native';
import Header from '../components/HeaderText';

const ChatListScreen = () => (
  <View>
    <Header>ChatListScreen</Header>
  </View>
);

export default ChatListScreen;
